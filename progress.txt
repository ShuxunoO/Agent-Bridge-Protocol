AGENT-BRIDGE — PROGRESS LOG
(newest first; one entry per working session)

== 2026-06-18 — Session 3: F1.1 Core validator + control-plane schemas + conformance ==
First implementation increment. Reference impl is TypeScript/Node (Node 24 native type-stripping,
ajv for JSON Schema 2020-12). Registry reachable; ajv installed.

Done (F1.1 PASSED):
- Authored the 10 remaining Core control-plane schemas in SPEC/schemas/core/: hello, hello_ack
  (carries inlined World Profile {id,version,hash,document}), roles_query, roles_list,
  pair_challenge, pair_request (target + optional pubkey/signature/nonce/claim), pair_result
  (session + capabilities + bound profile + expires_at), resume (last_event_seq), ping (shared by
  ping/pong), bye. Plus envelope fixes: "comment"->"$comment"; resume added to session-required.
- packages/validator (@agent-bridge/validator): CoreValidator validates the closed envelope,
  dispatches the payload to its Core schema by `type`, and enforces the §2 limits
  (size 65536B before parse, depth 16). Data-plane event/action are validated at the Core ENVELOPE
  layer only ({kind,seq,data}/{kind,data}); kind/data composition against a pinned World Profile is
  F1.2. x-abp-trust registered as a no-op ajv annotation keyword so profile schemas compile later.
  validateEncoded(text) covers the wire path (bytes -> parse -> depth -> schema); validateMessage(obj)
  the parsed path.
- Monorepo wiring: root package.json (npm workspaces) + validator package; init.sh recurses schemas
  and runs `npm test`; validator `test` runs tsc typecheck then node --test.

Evidence (./init.sh, fully green):
- 16 schemas parse (core x15 + social profile).
- tsc -p packages/validator/tsconfig.json: typecheck CLEAN.
- node --test: 25 tests, 25 pass, 0 fail. Covers: valid hello/hello_ack/roles_list/pair_result/
  event/action/ping/pong/error/resume pass; unknown type rejected; unknown envelope+payload field
  rejected (additionalProperties:false); wrong abp version rejected; bad enum rejected; event/action/
  resume without session rejected; event missing seq rejected; action missing data rejected; bye
  requires reason; non-object/missing-id rejected; oversize/non-JSON/over-depth rejected;
  encoded round-trip passes.

NEXT: F1.2 — World Profile loader: validate a profile doc against core/profile.json meta-schema;
pin by content hash (reject profile_mismatch; approval gate for unknown id,version,hash); COMPOSE
data-plane validation (event/action data vs pinned profile events[kind].data / actions[kind].data);
enumerate x-abp-trust:untrusted / client_authored field paths (feeds F4.1/F4.2). Then F2.1 transport.

== 2026-06-18 — Session 2: foundational protocol review + layering revision (F0.3) ==
User asked for a foundation-level review of SPEC/abp-v1.md before implementing, then chose the
layered fix. Review surfaced one root tension + several structural gaps; all addressed in-spec.

Root tension found: closed schemas (security) vs host diversity (the "any game/sim host" goal).
The data-plane vocabulary was AI-Town-shaped; §9 made every new world a Core version bump — so it
was not actually reusable. DECISION (user): plan B — split the protocol into two layers.

Done (F0.3 PASSED):
- ABP is now LAYERED: stable closed **Core** (envelope, control plane, event/action ENVELOPES) +
  per-world **World Profile** (the event/action kinds + data schemas) that the client PINS by
  content hash. New worlds = new profiles, never Core changes. Closed-schema guarantee holds per
  world (client only validates against a pinned profile). §0.3 + §5.
- Fixed: (2) "untrusted" was only prose -> now machine-marked `x-abp-trust` annotation (untrusted /
  client_authored) enumerable by the wrapper/DLP layers. (3) world.context was unbounded -> bounded
  (maxProperties, scalar leaves) + global message size(65536B)/depth(16) limits (§2,§5.4).
  (4) role discovery was an out-of-band URL -> in-band roles_query/roles_list + claim-based bind
  authorization + optional host-pubkey pinning (§4.2). (5) resume couldn't recover missed state ->
  global per-session event `seq` cursor + backfill-or-snapshot on resume (§4.3.2). Plus: relative
  deadlines, idempotency-by-id (§4.3.3), proactive-action capability, host-assigned conversation_id
  (§5.6.1), new error codes profile_unsupported/profile_mismatch.
- Schemas relocated/added: SPEC/schemas/core/{envelope,event,action,error,profile}.json +
  SPEC/schemas/profiles/social/1.json (the official abp.social/1 vocabulary w/ x-abp-trust +
  context bounds). Old flat schemas removed. init.sh now recurses.
- External section refs preserved: §0.2 (invariants), §6 (security), §9 (extensibility).

Evidence:
- `./init.sh` -> all 6 schemas parse (core x5 + social profile).
- python structural check: social/1.json conforms to core/profile.json meta-schema shape; every
  event/action entry has a `data` object; untrusted + client_authored annotations present.
  (Full JSON-Schema-2020-12 composition validation is F1.2's validator; jsonschema dep unavailable
  offline this session.)

NEXT: F1.1 — Core validator + remaining control-plane schemas (hello/hello_ack w/ inlined profile,
roles_*, pair_*, resume w/ last_event_seq, ping, bye) + conformance tests (unknown type/kind/fields
rejected; data-plane requires session; size/depth limits). Then F1.2 — profile loader + hash
pinning + composed data-plane validation + x-abp-trust path enumeration.

== 2026-06-18 — Session 1: protocol-first kickoff ==
Created the independent project (separate from ai-town) per user requirement: the connector +
protocol must be reusable by any host, not coupled to AI-Town.

Done:
- Project scaffold: README, CLAUDE.md (startup protocol), init.sh, DESIGN.md, feature_list.json.
- F0.1 PASSED: ABP v1 normative spec (SPEC/abp-v1.md) — envelope, control+data planes, closed
  event/action enums, 6 security invariants, 5-layer model ref, errors, conformance, extensibility.
- F0.2 PASSED: machine-readable closed schemas SPEC/schemas/{envelope,event,action,error}.json
  (JSON Schema 2020-12, additionalProperties:false). Verified parse via init.sh.

Decisions locked (see DESIGN.md §2): protocol-first; standalone repo; autonomy configurable;
curated persona memory; in-agent sandbox (OS-level optional); Claude Code + generic MCP first.

NEXT: F1.1 — schema validator + remaining control-plane schemas (hello, pair_*, resume, ping,
bye) + conformance tests (unknown type/kind/fields rejected; data-plane requires session).
This is the first implementation increment and is fully unit-testable (rules-based).
