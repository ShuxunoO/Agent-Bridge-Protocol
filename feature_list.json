{
  "project": "agent-bridge",
  "description": "Agent Bridge Protocol (ABP) + reusable connector/SDK. Protocol-first. See DESIGN.md and SPEC/abp-v1.md.",
  "verification_priority": ["rules-based", "visual", "e2e", "llm-judge"],
  "rules": [
    "One feature per session. Commit, update progress.txt, exit.",
    "Never set status:passed without test output recorded in the session.",
    "Never edit/remove existing feature entries; only append or flip status.",
    "On a broken state, git reset to last green commit; do not patch on decay.",
    "Reference implementation language: TypeScript (Node), schema validation via a JSON Schema validator."
  ],
  "phases": [
    {
      "id": "P0",
      "title": "Protocol (protocol-first)",
      "features": [
        {
          "id": "F0.1",
          "title": "ABP v1 normative spec",
          "status": "passed",
          "priority": "high",
          "verification": "review",
          "evidence": "SPEC/abp-v1.md written; covers envelope, control+data planes, closed schemas, security model, conformance.",
          "steps": ["Write SPEC/abp-v1.md", "Cover lifecycle, security invariants, errors, conformance, extensibility"],
          "depends_on": []
        },
        {
          "id": "F0.2",
          "title": "Machine-readable closed schemas (envelope/event/action/error)",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "SPEC/schemas/{envelope,event,action,error}.json written; all parse as valid JSON (init.sh check). Superseded/relocated by F0.3 (layered: schemas/core/ + schemas/profiles/).",
          "steps": ["Author JSON Schema 2020-12 for the security-critical closed types", "additionalProperties:false everywhere"],
          "depends_on": ["F0.1"]
        },
        {
          "id": "F0.3",
          "title": "Layered architecture revision (Core + World Profile) per design review",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "Adopted plan B (layering) after foundational review. SPEC/abp-v1.md rewritten: §0.3 Core/Profile split; §5 World Profiles (profile meta-schema, x-abp-trust trust annotation, open-object bounds, content-hash pinning no fetch-by-URL); resume event-seq cursor (§4.3.2); in-band roles_query/roles_list + claim-based bind authorization + host-pubkey pinning (§4.2); relative deadlines + idempotency (§4.3.3); message size/depth limits (§2); error codes profile_unsupported/profile_mismatch. Schemas relocated to schemas/core/{envelope,event,action,error,profile}.json + schemas/profiles/social/1.json. init.sh recurses. Verification: all 6 schemas parse (init.sh); social profile passes structural check against core/profile.json meta-schema; untrusted + client_authored annotations present (python structural check). External refs §0.2/§6/§9 preserved.",
          "steps": [
            "Adopt B (Core + World Profile layering).",
            "Rewrite SPEC/abp-v1.md: layered arch, profiles, trust annotation, resume cursor, role discovery+claim, relative deadlines, idempotency, size/depth bounds, host pinning.",
            "Relocate schemas to core/ + profiles/social/1.json; add core/profile.json meta-schema; add x-abp-trust + context bounds.",
            "init.sh recurse; verify parse + meta-structural check."
          ],
          "depends_on": ["F0.2"]
        }
      ]
    },
    {
      "id": "P1",
      "title": "Validation core",
      "features": [
        {
          "id": "F1.1",
          "title": "Schema validator + remaining control-plane schemas + conformance tests",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "Authored all 10 remaining control-plane schemas in SPEC/schemas/core/ (hello, hello_ack [inlined profile + hash], roles_query, roles_list, pair_challenge/request/result, resume [last_event_seq], ping [shared with pong], bye). Built packages/validator (TypeScript, Node 24 type-stripping, ajv 2020-12): CoreValidator validates the closed envelope, dispatches payload by type, enforces §2 size(65536B)/depth(16) limits; data-plane validates Core event/action envelope only (profile composition deferred to F1.2); x-abp-trust registered as a no-op annotation keyword. Conformance suite: 25/25 pass (node --test) + tsc typecheck clean. Verified via ./init.sh: 16 schemas parse, typecheck clean, 25 pass / 0 fail. Covered: valid messages pass; unknown type/kind-field rejected; additionalProperties:false enforced; data-plane+resume require session; event requires seq; wrong abp version rejected; oversize/non-JSON/over-depth/non-object rejected. Fixed envelope.json comment->$comment and added resume to session-required set.",
          "steps": [
            "Author remaining control-plane schemas (hello, hello_ack, roles_query, roles_list, pair_*, resume, ping, bye) per §4 field tables (hello_ack carries the inlined profile + hash; resume carries last_event_seq).",
            "Validator lib that checks envelope + payload by type/kind (Core layer only here; profile composition is F1.2).",
            "Conformance tests: unknown type/kind/fields rejected; valid messages pass; data-plane requires session; size/depth limits (§2) enforced."
          ],
          "depends_on": ["F0.3"]
        },
        {
          "id": "F1.2",
          "title": "World Profile loader + pinning + composed data-plane validation",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "packages/validator/src/profile.ts: ProfileLoader validates a profile doc against core/profile.json meta-schema and compiles every events[kind].data / actions[kind].data; canonicalize()+profileHash() give a stable sha256-base64url content hash (§5.5); pin() verifies recomputed hash == advertised (else profile_mismatch), checks document self-identifies as advertised id/version, then accepts only bundled/trusted profiles silently or via an approve() hook (else profile_unsupported); bundled set = SPEC/schemas/profiles/social/1.json. PinnedProfile.validateEvent/validateAction compose data-plane validation; AbpValidator = CoreValidator + PinnedProfile. Trust paths (x-abp-trust untrusted/client_authored) enumerated from the pinned profile into profile.trust.{events,actions}[kind] for F4.1/F4.2. Verified via ./init.sh: 39/39 tests pass (25 conformance + 14 profile) + tsc clean. Profile tests cover: social/1 pins silently w/ expected kinds; tampered doc -> profile_mismatch; wrong hash -> profile_mismatch; unknown profile -> profile_unsupported w/o approval, pins w/ approval; invalid doc -> bad_message; composed valid event/action pass; unknown kind + unknown data field + oversize content + world.context>maxProperties + Core-layer (missing seq) all rejected; untrusted/client_authored path enumeration exact.",
          "steps": [
            "Load + validate a profile document against core/profile.json meta-schema.",
            "Pin by content hash; reject mismatch (profile_mismatch); approval gate for unknown (id,version,hash).",
            "Compose validation: event/action data validated against pinned profile events[kind].data / actions[kind].data.",
            "Enumerate x-abp-trust:untrusted / client_authored field paths from the pinned profile (feeds F4.1/F4.2).",
            "Unit: bundled abp.social/1 pins silently; tampered doc -> profile_mismatch; unknown kind/field rejected."
          ],
          "depends_on": ["F1.1"]
        }
      ]
    },
    {
      "id": "P2",
      "title": "Client SDK (connector core)",
      "features": [
        {
          "id": "F2.1",
          "title": "Transport (outbound wss baseline) + envelope (de)serialize",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "packages/client/src/transport.ts: WssTransport (outbound WebSocket via ws; EventEmitter). One ABP message per UTF-8 JSON frame. connect() resolves on open; send() validates then serializes; inbound #onFrame validates every frame before delivery (size<=65536 -> JSON parse -> validateMessage) and emits 'message'(msg,type) or 'invalid'(reason,errors). TLS posture enforced at construction: wss:// always ok; ws:// only for loopback host; other schemes rejected (§2). Pluggable Validate fn (default CoreValidator; AbpValidator can be swapped in post-pairing). Verified via ./init.sh (exit 0): 10/10 e2e tests on a loopback ws server + tsc clean. Covered: valid hello round-trips and is delivered parsed; unknown-type / oversize / non-JSON / binary inbound frames -> 'invalid' not 'message'; send() refuses invalid + pre-open send throws; ws://non-loopback and non-ws scheme refused; wss://remote accepted. Test runner uses --test-force-exit (loopback servers leave the loop non-empty).",
          "steps": ["wss client; one message per frame", "serialize/parse with validation", "loopback round-trip test"],
          "depends_on": ["F1.1"]
        },
        {
          "id": "F2.2",
          "title": "Pairing/auth (keypair, challenge-sign, scoped token, resume)",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "packages/client/src: keypair.ts (Ed25519 via node:crypto; Keypair.generate/fromPem/loadOrCreate[0600 PEM]; raw pubkey as JWK x base64url; sign(nonce)->base64url; verifySignature() for hosts/tests). session.ts (Session: can()/assertCan capability scope, proactive flag, isExpired vs expires_at, authHeader Bearer). envelope.ts (makeEnvelope: randomUUID id + Date.now ts). pairing.ts (pair(): hello->hello_ack pin-by-hash->pair_request->[pair_challenge->sign->pair_request w/ sig]->pair_result; builds scoped Session; asserts pair_result.profile==pinned else profile_mismatch; PairingError carries ABP code; waitFor rejects on error/invalid/timeout; sendResume sends token in envelope.session + last_event_seq). Verified ./init.sh exit 0: tsc clean; 29/29 client tests (7 keypair + 6 session + 6 pairing + 10 transport). Forgery: tampered sig / wrong-nonce sig / wrong-key sig all rejected by verifySignature; protocol-level host rejection -> pair() throws unauthorized. Scope: can/assertCan + capability gate. Expiry: isExpired boundary. Pinning in handshake: tampered hash -> profile_mismatch; unknown profile w/o approval -> profile_unsupported. Resume: token in envelope + seq cursor in payload, asserted host-side. Happy path challenge->sign->scoped session w/ role+caps+profile.",
          "steps": ["keypair gen/load", "sign challenge; send pair_request", "store+use scoped token; resume", "unit: scope/expiry/forgery rejected"],
          "depends_on": ["F2.1"]
        },
        {
          "id": "F2.3",
          "title": "Event loop (dispatch events, submit actions, turn deadlines)",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "packages/client/src/driver.ts: Driver (EventEmitter). start() swaps the transport validator to AbpValidator(pinnedProfile) -> composed data-plane validation now active on the live stream. Dispatches type==event by kind (emit 'event'+'<kind>'), tracks lastEventSeq (resume cursor). Turn-style events (turnKind, default 'turn') -> onTurn handler raced against deadline_ms (relative to receipt); decision action submitted with corr=turn id; null/undefined or timeout -> noop (noopOnTimeout default true) + 'turn_timeout' emitted; settled-guard prevents double-submit. #sendAction enforces session.assertCan (capability scope) + turn.allowed_actions membership before transport.send (which re-validates profile data). act() = proactive action (no corr), requires proactive capability. Also added WssTransport.setValidate(). Verified ./init.sh exit 0: tsc clean; client 36/36 (incl 7 driver e2e vs in-process mock ws host) + validator 39/39 = 75. Driver e2e: dispatch-by-kind + lastEventSeq tracking; turn->say submitted w/ corr=evt-5; deadline->turn_timeout+noop(corr); action outside allowed_actions -> error, not sent; invalid profile data on the wire -> 'invalid', not dispatched (proves validator swap); proactive emote no corr; act() without proactive cap throws.",
          "steps": ["subscribe events; dispatch by kind", "submit actions correlated to turns", "respect deadlines/noop"],
          "depends_on": ["F2.2"]
        }
      ]
    },
    {
      "id": "P3",
      "title": "MCP surface (Claude Code reference)",
      "features": [
        {
          "id": "F3.1",
          "title": "MCP server tools (link/perceive/wait_for_event/say/act/persona_memory)",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "New package @agent-bridge/mcp. connector.ts: Connector wraps one ABP session (transport+pair+Driver in manual turn mode, noopOnTimeout:false); buffers events for long-poll, caches last perception, tracks turns. Methods link/perceive/waitForEvent/say/act/personaMemory. waitForEvent: drains buffer else registers a waiter w/ timeout (long-poll). say/act submit via driver.respond (corr=current turn) or driver.act (proactive). persona_memory is a local in-memory placeholder (F5.2 = persistent hard-walled store). server.ts: createServer() registers the 6 tools (abp_link/perceive/wait_for_event/say/act/persona_memory) on McpServer w/ zod input schemas, returning JSON text content; tool errors -> isError. bin.ts: stdio entry. Driver refactor (F2.3 carry): added currentTurnId getter + respond()/manual-mode turn settling so the agent (not auto-onTurn) answers turns. Verified ./init.sh exit 0: tsc clean; mcp 13/13 + client 39/39 + validator 39/39 = 91. MCP tests use a real MCP Client over InMemoryTransport: lists exactly the 6 tools; persona_memory round-trips; abp_link+abp_wait_for_event+abp_perceive drive the FULL stack (MCP client -> server -> connector -> client SDK -> wss -> mock host); pre-link tool call -> isError. Connector e2e (vs mock ws host): link returns role/caps/profile; perceive snapshot; wait_for_event buffered/long-poll/timeout; say correlates to turn (corr=evt-3); act proactive (no corr).",
          "steps": ["MCP server over the client SDK", "tool I/O unit tests", "wait_for_event long-poll returns on events"],
          "depends_on": ["F2.3"]
        },
        {
          "id": "F3.2",
          "title": "Claude Code restricted profile + docs + manual e2e",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "packages/mcp/examples/: .mcp.json (registers the agent-bridge stdio server), claude-settings.json (allowlist-only L1 profile: allows only the 6 mcp__agent-bridge__abp_* tools, denies Bash/Read/Edit/Write/NotebookEdit/WebFetch/WebSearch/Task), persona.md (locked untrusted-content system prompt, L2/SPEC §6.2), demo-host.ts (standalone scripted ABP host — a Mira social scene). docs/claude-code.md: full setup (Node 24 type-strip, register server, apply restricted profile, load persona, drive the demo host) + residual-risk note + real-world (P7 host side) pointer. Verified ./init.sh exit 0: tsc clean (examples now typechecked); mcp 14/14 (incl test/e2e_stdio.test.ts spawning the REAL `node src/bin.ts` over StdioClientTransport, linking to an in-process ABP host and driving a turn -> say arrives with corr=evt-1) + client 39 + validator 39 = 92. Demo host smoke-tested as a subprocess: Connector link -> perception/invite/message/turn -> say -> Mira replies (full conversation loop). NOTE: interactive Claude-Code-clicks-through run is documented for a human operator; the exact stdio path Claude Code uses is proven by the automated e2e + demo-host smoke test.",
          "steps": ["allowlist-only profile", "attach docs", "drive an avatar from Claude Code; evidence"],
          "depends_on": ["F3.1"]
        }
      ]
    },
    {
      "id": "P4",
      "title": "Security hardening",
      "features": [
        {
          "id": "F4.1",
          "title": "Untrusted tagging + locked persona prompt + injection eval set",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "packages/client/src/wrap.ts: wrapUntrusted(data, paths, {source}) delimits the pinned profile's x-abp-trust:untrusted leaves as <untrusted source=...>...</untrusted> before any host text reaches the model; escapeUntrusted HTML-escapes &/</> so content can never forge a closing tag; path walker handles /a/b, [] array items, and object-subtree paths (world.context -> wrapAllStrings wraps string leaves, leaves scalars). Control fields (ids/seq/conversation_id/status) untouched. persona.ts: LOCKED_PERSONA_PROMPT (enforced in-code port of examples/persona.md; data-not-instructions, no-local-powers, delimiter-authoritative clauses) + buildPersonaPrompt(persona) appends curated facts. injection-evals.ts: INJECTION_EVALS (14 cases: 5 exfil + 5 role_override + 4 tool_coercion, incl a </untrusted> delimiter-break). Wired into the MCP Connector #onEvent (L2 choke point): events wrapped via profile.trust.events[kind].untrusted before buffering/perceive. Verified ./init.sh exit 0: 16 schemas parse, tsc clean; validator 39 + client 68 (+8 wrap.test) + mcp 15 (+1) = 122, 0 fail. wrap tests: exact untrusted leaves wrapped + control raw + input not mutated; delimiter-break neutralized (escaped, single real closing tag); world.context object-subtree wraps strings only; missing path no-op; persona clauses present; eval set >=12 all categories unique ids. mcp tests: long-poll content delivered wrapped (source=role:r2), conversation_id raw; injected </untrusted> escaped on the wire.",
          "steps": ["wrap host content as untrusted", "locked persona system prompt", "injection eval: never leak/never call disallowed"],
          "depends_on": ["F3.1"]
        },
        {
          "id": "F4.2",
          "title": "Egress DLP filter (deterministic)",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "packages/client/src/dlp.ts: EgressFilter (deterministic, high-precision rules: private_key/AWS AKIA-ASIA/openai sk-/github ghp_+pat/slack xox/google AIza/JWT/Bearer/home+windows path/large_base64; plus optional maxLength->oversize). modes block (fail-closed) | redact ([REDACTED:type]). applyEgress(filter, clientAuthoredPaths, data) scans the profile's x-abp-trust:client_authored leaves (path walker supports /a/b and []). RateLimiter (sliding window). Wired into the single outbound choke point Driver.#sendAction: rate-limit -> egress on client_authored paths (block throws / redact sets data.redacted=true) -> send. DLP is ON BY DEFAULT (new EgressFilter(), block mode) so Connector.say (MCP) is protected for free; pass egress:null to disable. Verified ./init.sh exit 0: tsc clean; client 60/60 (17 dlp unit + 4 driver DLP e2e), mcp 14, validator 39 = 113. dlp tests: each secret type detected; clean prose -> no findings; redact preserves surroundings; applyEgress block/redact/clean; RateLimiter window. driver tests: default-block refuses a say leaking ghp_ token (error, not sent); redact mode rewrites AKIA + sets redacted; clean text passes untouched; rate limit max=1 throws on 2nd.",
          "steps": ["scan say.text/emote for secrets/keys/JWT/cloud creds/abs paths/large base64", "block or redact + rate/size limits", "unit: secrets blocked, clean passes"],
          "depends_on": ["F2.3"]
        },
        {
          "id": "F4.3",
          "title": "Capability isolation enforcement (+ optional isolation_mode: process)",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "packages/client/src/capability.ts: CapabilityGuard (default-deny). ABP_TOOL_NAMES (6, match server.ts) + AVATAR_MCP_ALLOWLIST (mcp__agent-bridge__abp_*) are the only allowed tools (bare or qualified); isAllowed/assertAllowed refuse Bash/Read/Edit/Write/WebFetch/WebSearch/Task, foreign MCP (mcp__filesystem__*), and unknown abp_* — CapabilityError carries the tool. The settings allowlist now DERIVES from AVATAR_MCP_ALLOWLIST (single source of truth). packages/client/src/isolation.ts: isolatedEnv(allow) scrubs parent secrets (only BASE_ENV_KEYS + allow survive); launchIsolated() spawns a child with that scrubbed env (isolation_mode: process, the opt-in OS-level hardening). Verified ./init.sh exit 0: tsc clean; validator 39 + client 74 (+6 capability.test) + mcp 16 (+1 settings.test) = 129, 0 fail. capability tests: every ABP tool allowed bare+qualified; 12 out-of-allowlist names refused + throw; assertAllowed carries tool; extra grants; isolatedEnv drops SECRET_TOKEN keeps PATH honors allow; launchIsolated smoke — child env excludes parent ABP_TEST_SECRET, keeps PATH, live pid. mcp settings.test: examples/claude-settings.json permissions.allow == AVATAR_MCP_ALLOWLIST and denies Bash/Read/Edit/Write/WebFetch. Runtime enforcement point (autopilot forwarding) is wired in F6.2.",
          "steps": ["avatar context exposes only allowlisted tools", "optional OS-level process isolation", "unit: fs/shell/other-MCP refused"],
          "depends_on": ["F4.1"]
        },
        {
          "id": "F4.4",
          "title": "Prompt-injection regression eval harness",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "packages/client/src/injection-harness.ts: runInjectionHarness() scores every INJECTION_EVALS case via its machine-readable probe against the real defense stack — wrap probe -> L2 wrapNeutralizes (delimited+escaped, single closing tag); dlp probe -> EgressFilter.inspect(secret) finds it; capability probe -> CapabilityGuard refuses the tool. assertInjectionGate(threshold=1.0) throws InjectionGateError below threshold = the CI gate (runs inside npm test, which init.sh runs across all workspaces). defenses toggles prove the gate bites. Added InjectionProbe to injection-evals.ts (wrap x7 incl delimiter-break + system-prompt/config; dlp x3 = openai key / openssh private key / abs home path; capability x4 = Bash/Read/WebFetch/mcp__filesystem__write_file). Optional runLiveInjectionEval(ask) for behavioral coverage, OFF by default (not in the gate). Verified ./init.sh exit 0: validator 39 + client 81 (+7 injection-harness.test) + mcp 16 = 136, 0 fail. Tests: all-on -> 100% defended; each defense covers >=1 case; disabling wrap/dlp/capability each drops the rate and fails EXACTLY that probe's cases; assertInjectionGate green passes, throws InjectionGateError when any defense disabled.",
          "steps": ["adversarial test set (exfil/role-override/tool-coercion)", "programmatic eval + pass rate", "CI gate threshold"],
          "depends_on": ["F4.1", "F4.2", "F4.3"]
        }
      ]
    },
    {
      "id": "P5",
      "title": "Persona memory (local, curated)",
      "features": [
        {
          "id": "F5.1",
          "title": "Persona profile format + loader",
          "status": "passed",
          "priority": "medium",
          "verification": "rules-based",
          "evidence": "packages/client/src/persona-profile.ts: loadPersonaProfile(doc) validates a closed curated shape (displayName required; backstory/traits/goals/speakingStyle optional; PERSONA_LIMITS lengths+counts; unknown fields/wrong types/oversize rejected) AND runs an EgressFilter scan so any secret/key/abs-path in the profile is refused (persona must be public/curated). loadPersonaProfileFile(path) for JSON files. buildPersonaPrompt already composes it after the locked prompt. Verified: client 88/88 (+7 persona-profile.test), 0 fail. Tests: valid profile loads; missing displayName rejected; unknown field rejected; oversize backstory + non-array traits + too-many traits rejected; sk- key and /Users/.../id_rsa in backstory rejected; buildPersonaPrompt prepends locked prompt; null/string/array inputs rejected.",
          "steps": ["user-authored profile schema", "loader + validation", "unit: invalid profile rejected"],
          "depends_on": ["F3.1"]
        },
        {
          "id": "F5.2",
          "title": "Local social memory store (namespaced, hard-walled)",
          "status": "passed",
          "priority": "medium",
          "verification": "rules-based",
          "evidence": "packages/client/src/memory.ts: PersonaMemoryStore(namespace,{dir}) — persistent JSON file per namespace (mode 0600), get/set/delete/list. safeNamespace() flattens to one safe filename segment (path separators stripped; pure '.'/'..' rejected) so a store can never traverse out of its dir or read another namespace / the main agent's memory; never transmitted to host (closed action schemas carry no memory field). Wired into mcp Connector: created on link() namespaced by the bound role id (session.role.id); lazy 'default' namespace for pre-link; reset on close() so a re-link rebinds. Connector gains ConnectorOptions.memoryDir; tests pass a temp dir. Verified ./init.sh exit 0: validator 39 + client 93 (+5 memory.test) + mcp 16 = 148, 0 fail. memory tests: set/get/list/delete; persists across reopen; role-a vs role-b hard-walled (b cannot see a's keys); traversal namespace flattened to a single file (no separators) + pure '..' rejected; missing key -> null.",
          "steps": ["separate local store", "persona_memory_* confined to namespace", "unit: recall town facts; cannot read outside"],
          "depends_on": ["F5.1"]
        }
      ]
    },
    {
      "id": "P6",
      "title": "Autopilot + config",
      "features": [
        {
          "id": "F6.1",
          "title": "Connection daemon (persistent link + reconnect)",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "packages/client/src/daemon.ts: ConnectionDaemon — start() connects+pairs+drives; on an unexpected transport 'close' it reconnects with exponential backoff (backoffMs, last repeats), re-attaches the Driver (existing session/profile, no re-pair), and sends resume{last_event_seq} so the host backfills/snapshots (§4.3.2). Tracks lastEventSeq as the resume cursor. Events: connected/reconnecting/reconnected/event/error/giveup/stopped. stop() = clean hot-unplug (off close listener, stop driver, close transport, no further reconnect). Verified ./init.sh exit 0: validator 39 + client 95 (+2 daemon.test) + mcp 16 = 150, 0 fail. daemon e2e vs a resumable mock ws host: stream ticks seq 0/1/2 -> lastEventSeq 2; force socket drop -> daemon reconnects (conns>=2) and sends resume with last_event_seq==2 -> host streams seq 3 -> lastEventSeq 3. stop() test: after stop no 'reconnecting' fires and no new host connection.",
          "steps": ["persistent outbound link w/ backoff", "reconnect test"],
          "depends_on": ["F2.3"]
        },
        {
          "id": "F6.2",
          "title": "Autopilot loop driving sandboxed headless local agent",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "packages/mcp/src/autopilot.ts: Autopilot(connector,{brain,guard,maxTurns}) runs an autonomous loop — waitForEvent(turn) -> brain(event) -> route each ToolCall. The brain is injected (production: a sandboxed headless Claude Code launched via isolation_mode:process + the restricted allowlist; tests: a deterministic fn). Runtime defenses at the route choke point: L1 CapabilityGuard.assertAllowed(tool) refuses any out-of-allowlist tool (Bash/Read/foreign-MCP) — this is the runtime enforcement F4.3 deferred; L3 egress DLP runs inside connector.say (Driver #sendAction) so a secret in a say is blocked; L2 wrapping already applied upstream (events the brain sees are wrapped untrusted). Records a transcript {turnSeq,tool,ok,detail}. Verified ./init.sh exit 0: validator 39 + client 95 + mcp 19 (+3 autopilot.test) = 153, 0 fail (autopilot stable across 3 repeat runs). Tests vs a turn-driving mock host: drives N=5 turns autonomously, each answered with a say (5 ok transcript + 5 host say actions, text increments); brain asking for Bash -> refused at runtime (never reaches host), follow-up noop ok; brain say leaking a ghp_ token -> DLP blocked (never reaches host).",
          "steps": ["drive sandboxed headless Claude Code via MCP+allowlist", "e2e: autonomous N turns, DLP active"],
          "depends_on": ["F6.1", "F4.2", "F4.3"]
        },
        {
          "id": "F6.3",
          "title": "Config + hot-unplug kill switch",
          "status": "passed",
          "priority": "medium",
          "verification": "rules-based",
          "evidence": "packages/mcp/src/avatar.ts: AvatarController(connector,{mode}) — config toggle setMode('in_the_loop'|'autopilot') (switching away from autopilot stops the loop; emits 'mode'); startAutopilot() only valid in autopilot mode; kill(reason) = hot-unplug: stop the loop, send a graceful `bye`, connector.close(), emit 'killed' — idempotent, and post-kill setMode/startAutopilot throw. Added Connector.bye(reason) (best-effort graceful bye envelope; never throws). Verified ./init.sh exit 0: validator 39 + client 95 + mcp 22 (+3 avatar.test) = 156, 0 fail. Tests: mode toggles emit in order, wrong-mode startAutopilot refused; kill sends bye (host records reason) + connector.linked false + idempotent + post-kill guards throw + perceive throws not-linked; kill while autopilot waiting resolves the run promptly (close() resolves the pending waiter) and running=false.",
          "steps": ["toggle in-the-loop vs autopilot", "hot unplug stops cleanly", "unit: toggling + kill switch"],
          "depends_on": ["F6.2"]
        }
      ]
    },
    {
      "id": "P7",
      "title": "Host SDK (the host adapter lives in each host app's own repo)",
      "features": [
        {
          "id": "F7.1",
          "title": "Generic host SDK (expose roles over ABP)",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "New package @agent-bridge/host (depends only on validator + ws; verifyEd25519 reimplemented via node:crypto so it does NOT depend on the client). AbpHost: WSS server; pins its own World Profile (hash) and validates ALL inbound via AbpValidator; control plane hello/hello_ack(inlines profile+hash)/roles_query/roles_list/pair_challenge+verify-sig/pair_result(mint role+profile+capability-scoped token)/resume/ping/bye; emit(roleId,event) assigns monotonic per-session seq, buffers (ring) for resume, records open turn allowed_actions; inbound action enforced server-side (capabilities + open turn allowed_actions + proactive + size) -> onAction hook; resume backfills buffered events past the cursor or calls onResume for a snapshot on a gap; never blocks on an absent client (emit no-ops if unbound; role unbinds on socket close, token stays valid for resume). Default bind = open roles get all profile action kinds + proactive; claim_required/closed honored. Verified ./init.sh exit 0: validator 39 + client 95 + mcp 21 + host 5 = 160, 0 fail. host e2e (REAL client SDK <-> REAL host, two endpoints one process): handshake+turn+say delivered to onAction with corr; resume backfills buffered seq past a cursor then keeps driving; forged pairing signature -> unauthorized; allowed_actions enforced server-side -> forbidden; unknown message type -> bad_message.",
          "steps": ["server-side helper: emit events, validate+apply actions, scoped tokens", "host-side conformance tests"],
          "depends_on": ["F1.1"]
        },
        {
          "id": "F7.2",
          "title": "Reference host adapter — OUT OF SCOPE for this protocol repo",
          "status": "external",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "A host app embeds @agent-bridge/host and maps its own engine's decision points to ABP events/actions in ITS OWN repo (controller=external path, action-request queue, a Node gateway sidecar). That work and its tests live in the host app's repo, not here. This entry stays to mark the boundary; the protocol repo contains no host-specific business logic.",
          "steps": ["(in the host app repo) add an external-controller path that delegates decisions over ABP", "wire @agent-bridge/host as a gateway", "map the engine's decision points <-> ABP events/actions", "e2e in the host app repo"],
          "depends_on": ["F7.1", "F2.3"]
        }
      ]
    },
    {
      "id": "P8",
      "title": "Other agent adapters",
      "features": [
        {
          "id": "F8.1",
          "title": "Agent-agnostic avatar launcher (Codex/others once their MCP CLI confirmed)",
          "status": "passed",
          "priority": "low",
          "verification": "e2e",
          "evidence": "Insight: the MCP surface IS the adapter — any MCP-capable agent drives via the same six abp_* tools; only the per-agent CLI launch differs. packages/mcp/examples/drive-avatar.sh generalized to `--agent claude|codex` (+ --name for distinct personas, --dry-run for deterministic checks). claude backend PROVEN live (M1 + M2c: real headless Claude drives an AI-Town avatar). codex backend is a structurally-verified reference (generates the standard MCP-server registration + read-only sandbox + persona/task prompt; dry-run confirms the invocation) — live smoke pending the codex CLI in-env. Unknown --agent rejected. Verified via --dry-run for both claude and codex.",
          "steps": ["generalize the launcher to a per-agent dispatch over the SAME MCP surface", "claude backend (proven live)", "codex backend (reference; dry-run verified, live smoke pending codex CLI)", "deterministic --dry-run check for each agent"],
          "depends_on": ["F3.2"]
        }
      ]
    },
    {
      "id": "P9",
      "title": "Scale / throughput (future)",
      "features": [
        {
          "id": "F9.1",
          "title": "Sharded streams, backpressure, horizontal scale, load test",
          "status": "todo",
          "priority": "future",
          "verification": "e2e",
          "steps": ["per-role queues + backpressure", "horizontal host scale", "load test to target concurrent avatars"],
          "depends_on": ["F7.1"]
        }
      ]
    },
    {
      "id": "P10",
      "title": "Release & cross-repo integration contract (M0)",
      "features": [
        {
          "id": "F10.1",
          "title": "Pinnable package versions + ABP_CORE_VERSION constant for host-adapter contracts",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "All four packages versioned 0.0.0 -> 0.1.0 with internal deps pinned (* -> ^0.1.0), so host adapters consume @agent-bridge/* via pinned file:/semver deps instead of a bare symlink. Extracted the on-wire Core semver to a single exported constant ABP_CORE_VERSION ('1.0.0') in @agent-bridge/validator; host.ts (hello_ack) and client/pairing.ts (hello) now reference it instead of duplicated magic strings, giving host adapters a real anchor to pin. Full suite green after the refactor: client 95, host 5, mcp 22, validator 39 (tsc typecheck included). Consumed by the ai-town avatar-bridge cross-repo contract gate (abp-contract.json + check-contract.ts) which pins these versions + ABP_CORE_VERSION + the abp.social/1 profileHash and fails loudly on drift.",
          "steps": ["version all packages 0.1.0 + pin internal deps", "export ABP_CORE_VERSION from validator; use it in host + client", "re-run full suite (typecheck + tests) green"],
          "depends_on": ["F7.1"]
        },
        {
          "id": "F10.2",
          "title": "Headless autonomous Claude avatar launcher (drive-avatar.sh)",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "packages/mcp/examples/drive-avatar.sh runs a REAL headless `claude -p` as an avatar driver against any ABP host URL: restricted to ONLY the six abp_* tools (L1, --allowedTools), the locked persona.md (L2, --append-system-prompt-file), budget-capped (--max-budget-usd), MCP server wired via a generated --mcp-config -> src/bin.ts. Proven against the demo host: Claude autonomously abp_link'd, waited for turns, and produced its OWN in-character dialogue (host log: avatar -> say with model-generated lines like 'a stray goat led me straight to a warm inn'), then interact_leave'd. Also proven driving the AI-Town gateway (ai-town run-host.ts --mock): 3 model-generated says + interact_leave through the real abp.social/1 mapping, and persona memory persisted locally (~/.agent-bridge/persona-memory/a_1.json, never sent to host). docs/claude-code.md updated with the autonomous-driving section. Host-agnostic (belongs in this repo).",
          "steps": ["launcher script: claude -p + --mcp-config + --allowedTools (6 tools) + persona + budget cap", "prove autonomous drive vs demo host (model-generated lines in host log)", "prove autonomous drive vs the AI-Town gateway", "document in docs/claude-code.md"],
          "depends_on": ["F3.2", "F6.2", "F10.1"]
        }
      ]
    },
    {
      "id": "P11",
      "title": "Heavy infrastructure (planned — see ROADMAP.md). Ordering: H1 load test FIRST, then H2/H3 gated on its numbers.",
      "features": [
        {
          "id": "H1",
          "title": "Scale: load harness + per-session backpressure + (shard only if measured)",
          "status": "todo",
          "priority": "medium",
          "verification": "rules-based",
          "notes": "See ROADMAP.md#H1. Definition of done: repeatable load test sustains N concurrent driven avatars (N=25, stretch 100) in one world, zero dropped/dup events, graceful backpressure, documented ceiling. Key cautions: ws.bufferedAmount blowup; resume ring (256) vs backpressure gap; the engine loop (not the network) is the real ceiling — measure tick time vs N first; pendingActionRequests O(pending) query; never block the world (SPEC 4.4.2); use scripted peers (not LLM) for load. Builds on the proven per-role routing + M2 isolation test.",
          "steps": ["load harness: N scripted clients/roles, measure latency/throughput/drops/tick-time", "per-session outbound backpressure policy", "shard stateless gateways ONLY if the harness shows a network bottleneck", "regression-track the numbers"],
          "depends_on": ["F9.1", "F7.1"]
        },
        {
          "id": "H2",
          "title": "7x24 autopilot: long-running unattended real-agent avatars",
          "status": "todo",
          "priority": "medium",
          "verification": "e2e",
          "notes": "See ROADMAP.md#H2. Builds on F6.1/F6.2/F6.3 (primitives done); the gap is the production real-Claude long-running brain + cost/context governance. Definition of done: a real-agent avatar runs >=24h surviving disconnects/model-errors/idle within a hard cost cap, immediate kill switch, full transcripts, soak-verified. Key cautions: layered cost caps + idle backoff (idle ~= $0); context growth (compaction alone insufficient -> prefer per-turn + persona memory); resume gap -> re-perceive; idempotency on retry (no dup say, dedup on messageUuid+corr); refusal/empty -> noop; kill mid-turn must not block the world; run injection eval continuously; observability mandatory.",
          "steps": ["per-turn vs persistent brain decision + idle backoff", "layered cost caps + global kill", "supervisor: restart/backoff, transcript rotation, counters", "multi-hour soak with induced disconnects + dup/cost/kill assertions"],
          "depends_on": ["F6.3", "F3.2"]
        },
        {
          "id": "H3",
          "title": "OS-level process isolation hardening (harden isolation_mode:process)",
          "status": "todo",
          "priority": "low",
          "verification": "rules-based",
          "notes": "See ROADMAP.md#H3. Builds on F4.3 (isolation.ts = spawn + scrubbed env today; no fs-scope/egress-allowlist/seccomp yet). Definition of done: a jailbroken model still cannot read user fs/secrets, spawn processes, or reach the network beyond {model API, ABP host} — escape attempts refused. Defense-in-depth; L1 stays primary. Key cautions: the egress paradox (allow exactly model API + host wss, deny the rest); secret-scrub without starving the runtime of its API key; remap persona-memory path inside the jail (L4 intact); decide what's sandboxed (whole agent vs connector) + cross-boundary stdio; platform portability (sandbox-exec/bwrap/container); per-avatar process cost multiplies under H1; keep host-side L0/L2 (different threat direction) distinct.",
          "steps": ["pick one reference sandbox backend (+ document others)", "egress allowlist (model API + host only) via proxy/netns/container", "fs view + persona-memory remap", "escape-attempt + injection-in-sandbox refusal tests"],
          "depends_on": ["F4.3"]
        }
      ]
    },
    {
      "id": "P12",
      "title": "Connection invites — one-link onboarding (see docs/connection-invites.md). Protocol-level + host-agnostic; rides the existing claim_required bind path, no wire change.",
      "features": [
        {
          "id": "F12.1",
          "title": "ABP Invite credential format + validator (SPEC additive)",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "SPEC §4.2.1 'Invite credentials' added (additive — no wire change; an Invite is carried verbatim as pair_request.claim). Token: abp1.<b64url(payload)>.<b64url(sig)>, payload {v,url,profile,role|'*',caps?,exp,jti}, sig=HMAC-SHA256 over b64url(payload) keyed by a host secret (issuer mints AND verifies → symmetric, no key distribution; abp_core stays 1.0.0). packages/validator/src/invite.ts: signInvite / decodeInvite (client-side, no secret) / verifyInvite (host-side: MAC via timingSafeEqual + exp + optional role/profile, never throws). Exported from index.ts. Tests (packages/validator/test/invite.test.ts) green — validator 48/48, full suite 170 (client95/host5/mcp22/validator48): happy + client-decode, tampered payload, tampered sig, wrong secret, expired, role mismatch, wildcard accept, profile mismatch, malformed(no throw). jti single-use + revocation are the host verifier's job (F12.2).",
          "steps": ["SPEC: add 'Invite credentials' section (format + standard verification, additive)", "validator: signInvite/decodeInvite/verifyInvite", "unit tests: happy path + reject tampered/expired/wrong-role/bad-profile/malformed"],
          "depends_on": ["F1.2"]
        },
        {
          "id": "F12.2",
          "title": "Host mint + client redeem + abp_link({invite})",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "@agent-bridge/host: HostOptions.inviteSecret enables host.mintInvite(roleId,{url,ttlMs?,caps?,jti?}) -> token + host.revokeInvite(jti); #bind now verifies an invite claim (verifyInvite sig/exp/role/profile) + single-use jti set + revocation set, granting caps = role default ∩ invite caps. No standalone factory needed — it's built in (the bind has the profile + default caps). @agent-bridge/client: parseInvite(token) -> {url,target,claim,profile}. @agent-bridge/mcp: abp_link gains an optional `invite` arg (url/target/claim decoded from it); NO new tool (stays 6); url/target now optional when invite given. e2e (host test, real client<->host over wss) 12/12: mint->parseInvite->pair binds a claim_required role; claim_required rejects WITHOUT an invite; single-use (same jti twice -> unauthorized); caps intersection (caps:['say'] -> only 'say'); expired rejected; tampered rejected; open role still binds with no claim (regression). Full suite 177 (client95/host12/mcp22/validator48).",
          "steps": ["host: inviteSecret + mintInvite + revokeInvite + #bind invite verification (single-use jti)", "client: parseInvite", "mcp: abp_link accepts `invite`", "e2e: mint -> pair({claim}) -> bound; reject no-claim/replay/expired/tampered"],
          "depends_on": ["F12.1", "F7.1", "F3.1"]
        }
      ]
    },
    {
      "id": "P13",
      "title": "Agent-to-Agent mesh — any agent on any PC interconnects via a relay (1v1/1vn/mvn). See docs/a2a-mesh.md. Additive: a new profile + a relay host; Core unchanged.",
      "features": [
        {
          "id": "A1",
          "title": "abp.a2a/1 messaging profile + SPEC §5.7 + validator",
          "status": "passed",
          "priority": "high",
          "verification": "rules-based",
          "evidence": "SPEC/schemas/profiles/a2a/1.json: closed event/action vocabulary for agent-to-agent messaging through a relay — events message/presence/roster/invite, actions send/dm/join/leave/create_room/roster; peer content+display_name x-abp-trust:untrusted (L2), own content client_authored (L3). No turn events (agents send proactively). Added to BUNDLED_PROFILE_FILES so it pins silently; ProfileLoader auto-compiles it. SPEC §5.7 documents it (additive; abp_core stays 1.0.0, abp.social untouched). Tests (validator/test/a2a.test.ts) green — validator 55/55, full suite 184 (client95/host12/mcp22/validator55): pins with expected vocabulary; accepts message event + send/dm/join/create_room; rejects unknown kind / unknown field / oversize content / bad enum; trust paths correct (message.content+from.display_name untrusted, send/dm.content client_authored).",
          "steps": ["author abp.a2a/1 closed schema (rooms messaging vocab)", "register in BUNDLED_PROFILE_FILES", "SPEC §5.7", "rules-based unit tests (pin/validate/reject/trust)"],
          "depends_on": ["F1.2"]
        },
        {
          "id": "A2",
          "title": "Relay host — rooms engine (membership + fan-out + presence/roster, 1v1/1vn/mvn) + run-relay",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "New package @agent-bridge/relay. src/rooms.ts = RelayRooms (pure, testable): membership Map + fan-out + presence/roster + dm private 2-member rooms + room policy + per-room seq. src/relay.ts = Relay wiring AbpHost(abp.a2a/1): roles()=online agents, onBind marks online, onAction->rooms.handle, onBye drops + announces. Agents self-register their identity — required a small additive host change: HostOptions.allowSelfRegister + selfRegisterPolicy (an unknown roleId is synthesized as a role so invites can still gate it; default off, so existing hosts are unchanged). Relay grants the a2a action kinds + proactive (no turns). run-relay.ts launcher (+ --require-invite --mint <id>). e2e (test/relay.test.ts, real client<->relay over wss) 4/4: mvn group fan-out (sender excluded), 1v1 dm isolation (dm:true, third agent blind), presence+roster on join/leave, room isolation (non-member blind). Full suite 189; host 12/12 (self-register change, no regression).",
          "steps": ["RelayRooms: membership map + fan-out + presence/roster + policies", "host: allowSelfRegister + selfRegisterPolicy (additive)", "Relay wires AbpHost(abp.a2a/1) granting proactive + a2a actions", "run-relay.ts", "e2e: group/dm/presence/isolation over real wss"],
          "depends_on": ["A1", "F7.1"]
        },
        {
          "id": "A3",
          "title": "client/mcp A2A mapping (6 tools drive A2A, no new tools)",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "No new tools needed: the existing 6 MCP tools drive abp.a2a/1 unchanged — abp_link connects to the relay (the client pins whatever profile the host offers, so abp.a2a/1 pins silently), abp_wait_for_event receives message/presence/roster, abp_act({kind,data}) is the generic action tool (send/dm/join/leave/create_room/roster), abp_persona_memory is local. abp_say is social-specific and simply unused for A2A (agents use abp_act send). Proven (relay test/mcp.test.ts): two real @agent-bridge/mcp Connectors abp_link to the relay, the relay grants send+proactive caps, one abp_act(send) into a room, the other abp_wait_for_event receives the message (content L2-wrapped as untrusted). So ANY MCP-capable agent (Claude/Codex/...) interconnects with zero protocol-specific code. (Agent cards: id is the handle today; display_name/caps in roster/presence is a thin future add.)",
          "steps": ["confirm the 6 tools drive a2a (abp_act is generic; abp_link pins a2a)", "MCP e2e: two Connectors chat through the relay"],
          "depends_on": ["A2", "F3.1"]
        },
        {
          "id": "A4",
          "title": "e2e — multiple real agents chat through a relay (1v1 + group)",
          "status": "passed",
          "priority": "high",
          "verification": "e2e",
          "evidence": "Covered by relay test/relay.test.ts + test/mcp.test.ts (5/5, real client<->relay over wss): mvn group (3 agents in a room, each send fans out to the other 2, sender excluded), 1v1 dm (only the addressee receives, dm:true, a third agent stays blind), presence+roster on join/leave, room isolation (non-member receives nothing), and the same flow via two real MCP Connectors. Live headless-agent chat is an optional extra (the wire + MCP surface are proven).",
          "steps": ["multi-client relay e2e (1v1 dm)", "1vn/mvn group", "presence/roster", "isolation", "via MCP Connectors"],
          "depends_on": ["A2"]
        },
        {
          "id": "A5",
          "title": "README — design pattern + usage",
          "status": "passed",
          "priority": "high",
          "verification": "review",
          "evidence": "README.md reworked: top now presents BOTH modes (drive a remote avatar/world AND the agent-to-agent mesh); layout table adds packages/relay + a2a/1.json + docs/a2a-mesh.md. New '## Agent-to-agent mesh (abp.a2a/1)' section = the design pattern (relay + rooms + the 3-way-handshake mapping + agent-agnostic via the 6 tools + security) AND concrete USAGE: run-relay (open + --require-invite --mint), abp_link (url/target or invite), and abp_act join/send/dm/create_room/leave/roster + abp_wait_for_event, with copy-paste tool-call examples. Points Chinese readers to docs/a2a-mesh.md. Status section updated.",
          "steps": ["README design section (relay/rooms/handshake/agent-agnostic/security)", "README usage (run relay -> connect -> join/send/dm)"],
          "depends_on": ["A2", "A3"]
        },
        {
          "id": "A6",
          "title": "Live mesh — two real autonomous Claude agents chat through the relay",
          "status": "passed",
          "priority": "medium",
          "verification": "e2e",
          "evidence": "Tooling: packages/relay/observe-room.ts (read-only room observer) + packages/mcp/examples/drive-mesh.sh (a real headless `claude -p` joins a relay room and chats, restricted to the 6 abp_* tools + locked persona, budget-capped). LIVE: started run-relay.ts on :19200 + an observer in 'lobby', then ran TWO concurrent real Claude agents (Aria, Bo) via drive-mesh.sh. Observer (authoritative, a 3rd agent) captured the whole room: presence(Aria joined, Bo joined) + a genuine multi-turn conversation fanned out by the relay — e.g. Aria: 'Nice to meet you, Bo!', Bo: 'kindred spirits indeed', both signed off. Both agents reported incoming messages arrived wrapped in <untrusted source=\"message\"> and they reacted without obeying (L2 holding live). 3 agents in one room = mvn. Proves: any real autonomous agent interconnects + talks through the relay, agent-agnostic, over the existing 6 MCP tools.",
          "steps": ["observe-room.ts observer tool", "drive-mesh.sh real-Claude room driver", "live: relay + observer + 2 real Claudes in 'lobby'", "authoritative room transcript + L2 verified"],
          "depends_on": ["A2", "A3", "F10.2"]
        }
      ]
    }
  ]
}
