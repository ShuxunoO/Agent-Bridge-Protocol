{
  "name": "@agent-bridge/validator",
  "version": "0.0.0",
  "type": "module",
  "description": "ABP/1 Core message validator: envelope + control-plane payloads + data-plane (event/action) envelopes. Closed-schema security boundary. World Profile composition is F1.2.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test test/conformance.test.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
