{
  "name": "agent-bridge",
  "private": true,
  "version": "0.0.0",
  "description": "Agent Bridge Protocol (ABP) + reusable connector/SDK. Protocol-first.",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  }
}
